function Landscape(width,height){
    return (width > height);     // here no need to add this ? true : false; since without this also it returns true or false as (a>b) condition returns boolean
}

console.log(Landscape(750,1150));